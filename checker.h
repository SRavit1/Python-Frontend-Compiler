#include "nodescpp.h"

//Returns whether statements make sense
int checkProgram(const std::vector<function_exp> statements) {
	//check whether return type of function expression is correct

	return 1;
}